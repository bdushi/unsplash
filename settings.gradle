pluginManagement {
    enableFeaturePreview('VERSION_CATALOGS')
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
    versionCatalogs {
        libs {
            version('kotlin', '1.6.10')
            version('compose', '1.2.0-alpha01')
            version('paging', '3.1.0')
            version('dagger', '2.39.1')
            version('datastore', '1.0.0')
            version('retrofit', '2.9.0')
            version('rxjava3', '3.1.3')
            version('rxjava3-android', '3.0.0')
            version('picasso', '2.8')
            version('viewpager', '1.0.0')
            version('swiperefreshlayout', '1.0.0')
            version('recyclerview', '1.2.1')

            alias('kotlin-stdlib').to('org.jetbrains.kotlin', 'kotlin-stdlib').versionRef('kotlin')
            alias('appcompat').to('androidx.appcompat:appcompat:1.4.0')
            alias('android-core').to('androidx.core:core-ktx:1.7.0')
            alias('lifecycle').to('androidx.lifecycle:lifecycle-runtime-ktx:2.4.0')
            alias('google-android-material').to('com.google.android.material:material:1.6.0-alpha02')
            alias('viewpager2').to('androidx.viewpager2', 'viewpager2').versionRef('viewpager')
            alias('recyclerview').to('androidx.recyclerview', 'recyclerview').versionRef('recyclerview')
            alias('swiperefreshlayout').to('androidx.swiperefreshlayout', 'swiperefreshlayout').versionRef('swiperefreshlayout')
            /**
             * Paging
             */
            alias('paging3').to('androidx.paging', 'paging-runtime').versionRef('paging')
            alias('paging3-common').to('androidx.paging', 'paging-common-ktx').versionRef('paging')
            alias('paging3-rxjava3').to('androidx.paging', 'paging-rxjava3').versionRef('paging')
            /**
             *
             */
            alias('desugar_jdk_libs').to('com.android.tools:desugar_jdk_libs:1.1.5')
            alias('datastore-core').to('androidx.datastore', 'datastore-core').versionRef('datastore')
            alias('datastore-preferences').to('androidx.datastore', 'datastore-preferences').versionRef('datastore')
            alias('protobuf').to('com.google.protobuf:protobuf-javalite:3.14.0')
            /**
             * RxJava3
             */
            alias('rxjava3').to('io.reactivex.rxjava3', 'rxjava').versionRef('rxjava3')
            alias('rxjava3-android').to('io.reactivex.rxjava3', 'rxandroid').versionRef('rxjava3-android')
            /**
             * Retrofit Version Catalog
             */
            alias('retrofit').to('com.squareup.retrofit2', 'retrofit').versionRef('retrofit')
            alias('converter-gson').to('com.squareup.retrofit2', 'converter-gson').versionRef('retrofit')
            alias('logging-interceptor').to('com.squareup.okhttp3:logging-interceptor:5.0.0-alpha.3')
            alias('picasso').to('com.squareup.picasso', 'picasso').versionRef('picasso')
            /**
             * Dagger Version Catalog
             */
            alias('dagger2').to('com.google.dagger', 'dagger').versionRef('dagger')
            alias('dagger2-compiler').to('com.google.dagger', 'dagger-compiler').versionRef('dagger')
            alias('dagger2-android').to('com.google.dagger', 'dagger-android').versionRef('dagger')
            alias('dagger2-android-support').to('com.google.dagger', 'dagger-android-support').versionRef('dagger')
            alias('dagger2-processor').to('com.google.dagger', 'dagger-android-processor').versionRef('dagger')
            /**
             * Test Version Catalog
             */
            alias('junit').to('junit:junit:4.13.2')
            alias('ext-test').to('androidx.test.ext:junit:1.1.3')
            alias('espresso-test').to('androidx.test.espresso:espresso-core:3.4.0')
        }
    }
}
rootProject.name = "unsplash"
include ':app'
include ':unsplash'
include ':di'
include ':adapter'
